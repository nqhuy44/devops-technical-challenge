apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment-blue
  labels:
    app: webapp
    environment: prod
    version: blue
  namespace: webapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
        environment: prod
        version: blue
    spec:
      containers:
      - name: webapp
        image: webapp-image:prod-blue
        ports:
        - name: webapp
          containerPort: 3000
        envFrom:
        - configMapRef:
            name: webapp-config
        livenessProbe:
          httpGet:
            path: /health
            port: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment-green
  labels:
    app: webapp
    environment: prod
    version: green
  namespace: webapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
        environment: prod
        version: green
    spec:
      containers:
      - name: webapp
        image: webapp-image:prod-green
        ports:
        - name: webapp
          containerPort: 3000
        envFrom:
        - configMapRef:
            name: webapp-config-green
        livenessProbe:
          httpGet:
            path: /health
            port: 3000